# Demo mode (set to 'true' to run without external services)
DEMO_MODE=true
PORT=1234

# Admin key for event creation endpoint
ADMIN_KEY=

# Admin phone number for alerts
ADMIN_PHONE=

# ─── Only needed when DEMO_MODE=false ─────────────────────────────────────

# Twilio - https://console.twilio.com
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE_NUMBER=

# Stripe - https://dashboard.stripe.com/apikeys
STRIPE_ACCOUNT_ID=
STRIPE_SECRET_KEY_TEST=
STRIPE_SECRET_KEY_LIVE=

# Firebase service account keys must be present as:
#   backend/prod_serviceAccountKey.js (or .json)
#   backend/dev_serviceAccountKey.js (or .json)
# These files are gitignored and must be created manually.
